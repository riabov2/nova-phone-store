"use client";import {useState} from "react";import PhoneVisual from "./PhoneVisual";
const views=[{side:"front" as const,label:"Front"},{side:"back" as const,label:"Rear"},{side:"front" as const,label:"Display"}];
export default function ProductGallery(){const [active,setActive]=useState(0);const [zoom,setZoom]=useState(false);return <div className="gallery"><div className={`gallery-stage ${zoom?"zoom":""}`} onClick={()=>setZoom(!zoom)}><span className="gallery-count">0{active+1} / 03</span><PhoneVisual side={views[active].side}/><button aria-label={zoom?"Zoom out":"Zoom product image"} className="zoom-button">{zoom?"−":"+"}</button></div><div className="thumbs">{views.map((v,i)=><button onClick={()=>setActive(i)} className={active===i?"active":""} key={v.label}><PhoneVisual side={v.side}/><span>{v.label}</span></button>)}</div></div>}
